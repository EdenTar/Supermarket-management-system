package PresentationLayer.EmployeTransportFrontend;

import Backend.ServiceLayer.Result.Response;
import Backend.ServiceLayer.Result.Result;

public interface ServiceFunctionHelp {
    Result apply(String[] values);
}
